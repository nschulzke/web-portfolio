<div class="card">
  <%= image_tag portfolio_item.thumb_image if portfolio_item.thumb_image? %>
  <p class="card-text">
    <%= link_to portfolio_item.title, show_portfolio_path(portfolio_item) %>
    <%= portfolio_item.subtitle %>
  </p>
</div>